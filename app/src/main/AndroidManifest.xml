<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.textconversion_simulation">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <meta-data
        android:name="com.google.android.gms.ads.APPLICATION_ID"
        android:screenOrientation="portrait"
        android:value="ca-app-pub-3940256099942544~3347511713"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TextConversionSimulation">
        <activity
            android:name=".TextConverter"
            android:label="@string/title_activity_text_converter"
            android:theme="@style/Theme.TextConversionSimulation.NoActionBar"></activity>
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>